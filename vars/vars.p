#include float


main(){
    // str
    new text{} = "hello"
    // number
    new num = 1
    // float
    new Float: a = 0.2
    // boolean
    new bol =  true
    if(bol) {
        printf("%s %d %f %d", text, num, a, bol )
    }

}
